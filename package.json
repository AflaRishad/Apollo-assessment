{
  "name": "energyops",
  "private": true,
  "workspaces": [
    "apollo/frontend"
  ],
  "scripts": {
    "dev": "npm --workspace apollo/frontend run dev",
    "build": "npm --workspace apollo/frontend run build",
    "preview": "npm --workspace apollo/frontend run preview"
  }
}
